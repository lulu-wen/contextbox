這個資料夾是 cleanup A 線的固定樣本。測試本身會用暫存資料夾動態建立檔案，
這裡留給 B/C 接假資料與 demo script 用。

- `duplicate-a.bin`、`duplicate-b.bin`：同內容重複檔。
- `old-archive.zip`：舊壓縮檔樣本。
- `partial-download.crdownload`：下載中斷樣本。
- `normal-document.pdf`：保護副檔名樣本，預設不該被低信心規則勾選。
